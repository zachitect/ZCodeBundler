<Window x:Class="ZCodeBundler.Dialogs.TreeSelection.TreeSelectionWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Background="White"
        Height="600"
        Width="800"
        Title="Selection" MinHeight="300" MinWidth="500"
        Topmost="True"
        WindowStyle="ToolWindow"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="105"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="10,5,10,5">
            <TextBlock Name ="TextTitle" Foreground ="#424242" HorizontalAlignment="Center" FontSize="16" Text="User Operation Window" FontWeight="Bold"/>
            <TextBlock TextWrapping ="WrapWithOverflow" Name ="TextInstruction" Foreground ="#535353" HorizontalAlignment="Center" FontSize="12"  Text="Please proceed with user operation"/>
        </StackPanel>
        <!--Main Tree View-->
        <Border Grid.Row="1" Background="White" BorderBrush="#222" BorderThickness="0,0.5,0.5,0.5" Margin="0,0,5,0">
            <TreeView Name="MainTreeView" Background="White" BorderBrush="#222"
                      ItemsSource="{Binding RootNodes}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.CanContentScroll="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.IsVirtualizingWhenGrouping="True">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox
                                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                IsThreeState="True"
                                />
                            <TextBlock Text="{Binding DisplayName}" Margin="5,0,0,0"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>
        <!--Main Tree View Above-->
        <Grid Grid.Row="1" Grid.Column="1" Margin="0,0,5,0">
            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                <Button Margin="5,5,5,5" Height="40" Grid.Row="2" Grid.Column="0" Name="ButtonSelectAll" Content="Select All" Click="ButtonSelectAll_Click"/>
                <Button Margin="5,5,5,5" Height="40" Grid.Row="2" Grid.Column="0" Name="ButtonSelectNone" Content="Select None" Click="ButtonSelectNone_Click"/>
                <Border Height="30"/>
                <Button Margin="5,5,5,5" Height="40" Grid.Row="2" Grid.Column="0" Name="ButtonExpandAll" Content="Expand All" Click="ButtonExpandAll_Click"/>
                <Button Margin="5,5,5,5" Height="40" Grid.Row="2" Grid.Column="0" Name="ButtonCollapseAll" Content="Collapse All" Click="ButtonCollapseAll_Click"/>
            </StackPanel>
        </Grid>
        <Button Margin="5,5,5,5" Height="40" Grid.Row="2" Grid.Column="0" Name="ButtonProceed" Content="Proceed" Click="ButtonProceed_Click"/>
        <Button Margin="0,0,5,0" Height="40" Grid.Row="2" Grid.Column="1" Name="ButtonCancel" Content="Cancel" Click="ButtonCancel_Click"/>
    </Grid>
</Window>