<Window x:Class="ZCodeBundler.Dialogs.MultiSelectionWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Background="White"
        Height="400"
        Width="600"
        Title="Selection" MinHeight="300" MinWidth="300"
        Topmost="True"
        WindowStyle="ToolWindow"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Margin="10,5,10,5">
            <StackPanel>
                <TextBlock Name ="TextTitle" Foreground ="#424242" HorizontalAlignment="Center" FontSize="16" Text="User Operation Window" FontWeight="Bold"/>
                <TextBlock TextWrapping ="WrapWithOverflow" Name ="TextInstruction" Foreground ="#535353" HorizontalAlignment="Center" FontSize="12"  Text="Please proceed with user operation"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="1">
            <ListBox Name="MainListBox" SelectionMode="Extended"
                    Background="White"
                    BorderThickness="0,0.5,0,0.5"
                    BorderBrush="#222"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.CanContentScroll="True"
                    VirtualizingPanel.IsVirtualizing="True"
                    VirtualizingPanel.VirtualizationMode="Recycling"
                    VirtualizingPanel.IsVirtualizingWhenGrouping="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding}"
                                 IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}"
                                 IsHitTestVisible="False"
                                 Focusable="False"
                                 Margin="5,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>
        <Border Grid.Row="2" Height="40">
            <Grid Margin="5,5,5,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Name="ButtonSelectAll" Content="All" Margin="0,0,0,0" Click="ButtonSelectAll_Click"/>
                <Button Grid.Column="1" Name="ButtonSelectNone" Content="None" Margin="5,0,0,0" Click="ButtonSelectNone_Click"/>
                <Button Grid.Column="2" Name="ButtonProceed" Content="Proceed" Margin="5,0,0,0" Click="ButtonProceed_Click"/>
                <Button Grid.Column="3" Name="ButtonCancel" Content="Cancel" Margin="5,0,0,0" Click="ButtonCancel_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>